spring:
  data:
    mongodb:
      uri: mongodb://admin:admin@localhost:27017/db_event
      database: db_event

server:
  port: 8080

feign:
  client:
    config:
      default:
        connectTimeout: 5000
        readTimeout: 5000

logging:
  level:
    org.springframework.data.mongodb.core: DEBUG

